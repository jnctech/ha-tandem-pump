sonar.organization=jnctech
sonar.projectKey=jnctech_ha-tandem-pump

sonar.sources=.
sonar.python.coverage.reportPaths=coverage.xml

sonar.exclusions=tests/**,**/__pycache__/**,**/node_modules/**
sonar.coverage.exclusions=tests/**

# Quality gate targets (gate configured in SonarCloud UI — documented here for visibility)
# Coverage:            >= 80% line coverage (excluding tests/)
# Cognitive complexity: < 15 per function
# Duplication:         0%
# Bugs / Vulnerabilities / Code Smells: Grade "A" on all categories
